# 🚀 AI新闻助手 - GitHub Pages 部署指南

## ❌ 问题说明
当前 GitHub Pages 显示的是 README 内容，而不是 React 应用界面。

## ✅ 原因分析
- React 应用需要先构建（build），生成静态文件
- 这些静态文件需要部署到 `gh-pages` 分支
- GitHub Pages 需要设置为从 `gh-pages` 分支读取内容

---

## 📋 完整部署步骤

### 步骤 1：确认依赖已安装
```bash
# 确保 gh-pages 已安装
npm install
```

### 步骤 2：运行部署命令
```bash
# 这个命令会自动构建并部署
npm run deploy
```

**这个命令会做什么？**
1. 运行 `npm run build` 构建生产版本
2. 将 build 文件夹的内容推送到 `gh-pages` 分支
3. 自动创建 gh-pages 分支（如果不存在）

### 步骤 3：配置 GitHub Pages
1. 访问您的 GitHub 仓库
2. 点击 **Settings**（设置）
3. 在左侧菜单找到 **Pages**
4. 在 **Source**（源）部分：
   - **Branch**: 选择 `gh-pages`
   - **Folder**: 选择 `/ (root)`
5. 点击 **Save**（保存）

### 步骤 4：等待部署完成
- GitHub Actions 会自动部署（需要 1-3 分钟）
- 部署完成后访问：https://yhao-l.github.io/ai-news-assistant/

---

## 🖥️ Windows 快速部署脚本

使用 `deploy-github.bat` 一键部署：
```bash
deploy-github.bat
```

---

## 🔍 验证部署状态

### 1. 检查 gh-pages 分支是否创建
```bash
git branch -a
```
应该能看到 `remotes/origin/gh-pages`

### 2. 查看 GitHub Actions
- 访问仓库的 **Actions** 标签
- 查看 **pages build and deployment** 工作流
- 确保状态为绿色（成功）

### 3. 访问网站
https://yhao-l.github.io/ai-news-assistant/

---

## ⚠️ 常见问题

### 问题 1：npm run deploy 失败
**错误**: `gh-pages not found`
**解决**:
```bash
npm install gh-pages --save-dev
```

### 问题 2：网站显示 404
**原因**: GitHub Pages 未正确配置
**解决**: 按照步骤 3 重新配置 Pages 设置

### 问题 3：页面显示空白
**原因**: homepage 配置不正确
**解决**: 确认 package.json 中的 homepage 为：
```json
"homepage": "https://yhao-l.github.io/ai-news-assistant"
```

### 问题 4：样式丢失
**原因**: 路径问题
**解决**: 确保所有资源使用相对路径

---

## 📝 package.json 配置说明

当前配置（已正确）：
```json
{
  "homepage": "https://yhao-l.github.io/ai-news-assistant",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
```

---

## 🔄 更新应用流程

每次修改代码后：
```bash
# 方法 1：使用 npm
npm run deploy

# 方法 2：使用 Windows 脚本
deploy-github.bat
```

**不需要**手动构建或推送到 main 分支，`npm run deploy` 会自动处理一切！

---

## 📊 部署架构

```
本地代码
   ↓
npm run build (构建)
   ↓
build/ 文件夹
   ↓
gh-pages -d build (推送到 gh-pages 分支)
   ↓
GitHub Pages (从 gh-pages 分支部署)
   ↓
公网访问 🌐
```

---

## ✅ 部署检查清单

- [ ] 已安装 node_modules (`npm install`)
- [ ] package.json 中 homepage 配置正确
- [ ] 运行 `npm run deploy` 无错误
- [ ] GitHub 仓库中存在 gh-pages 分支
- [ ] GitHub Pages 设置指向 gh-pages 分支
- [ ] 网站可以正常访问

---

## 🎉 成功标志

当您看到以下内容时，说明部署成功：
1. 命令行显示 `Published`
2. GitHub Pages 显示绿色勾号
3. 访问网址能看到 **AI新闻助手** 应用界面（不是 README）

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. Git 远程仓库配置：`git remote -v`
2. 构建是否成功：`npm run build`
3. GitHub Actions 日志
4. 浏览器控制台错误信息
